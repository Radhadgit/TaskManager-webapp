# ============================================
# Task Manager App - Environment Configuration
# ============================================
# 
# Copy this file to .env and update the values according to your environment
# 
# IMPORTANT: Never commit the .env file to version control!
# The .env file is already included in .gitignore
#
# ============================================

# ============================================
# Application Environment
# ============================================
# Options: development, production, test
# Controls the application behavior and logging level
NODE_ENV=development

# ============================================
# Server Configuration
# ============================================
# Port on which the Next.js server will run
# Default: 3000
PORT=3000

# Hostname for the server
# Use 0.0.0.0 to accept connections from all network interfaces
# Default: 0.0.0.0
HOSTNAME=0.0.0.0

# ============================================
# Database Configuration
# ============================================
# MongoDB Connection String
# 
# Local MongoDB:
#   mongodb://localhost:27017/taskmanager
#
# MongoDB with Authentication:
#   mongodb://username:password@localhost:27017/taskmanager?authSource=admin
#
# MongoDB Atlas (Cloud):
#   mongodb+srv://username:password@cluster.mongodb.net/taskmanager?retryWrites=true&w=majority
#
# Docker Compose (if using docker-compose.yml):
#   mongodb://mongodb:27017/taskmanager
#
# Example for local development:
MONGODB_URI=mongodb://task-manager-mongodb:27017/taskmanager


# ============================================
# Authentication & Security
# ============================================
# JWT Secret Key for token signing and verification
# 
# IMPORTANT SECURITY NOTES:
# - Use a strong, random string (minimum 32 characters recommended)
# - Generate a secure secret: openssl rand -base64 32
# - NEVER use the default value in production
# - Keep this secret secure and never commit it to version control
# - Use different secrets for development, staging, and production
#
# Generate a secure secret:
#   $ openssl rand -base64 32
#   or
#   $ node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# Example (REPLACE WITH YOUR OWN SECURE SECRET):
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters

# JWT Token Expiration (Optional)
# Default: 7d (7 days)
# Format: number followed by unit (s, m, h, d)
# Examples: 1h, 24h, 7d, 30d
# JWT_EXPIRES_IN=7d

# ============================================
# Optional: Application URLs
# ============================================
# Base URL of your application (for production)
# Used for generating absolute URLs in emails, etc.
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Optional: Logging
# ============================================
# Enable detailed logging (development only)
# LOG_LEVEL=debug

# ============================================
# Production Environment Example
# ============================================
# For production, use strong secrets and secure MongoDB:
#
# NODE_ENV=production
# PORT=3000
# HOSTNAME=0.0.0.0
# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/taskmanager?retryWrites=true&w=majority
# JWT_SECRET=<generate-strong-secret-using-openssl-or-crypto>
# NEXT_PUBLIC_APP_URL=https://yourdomain.com
